Payment request params:
 user_defined: obj of udf[0-9]:
 card_discounts: array of obj of [discount_cards, discount_title, discount_amount/discount_percent]
 token_info:
 payment_token: string
 card_details: obj of [pan, cvv, expiry_year, expiry_month]
 apple_pay_token: obj
 donation:
 invoice object:
 customer_ref: string
 BIN based card approval:


Discount Patterns:


Builders:
  Own Form:
  Managed Form:
  ApplePay:


Requests:
  Invoice cancel:
  Invoice SMS send:
  Invoice search:
  Invoice Mark as Read:
  valU inquiry:
  Sadad inquiry:


Callback & Return:
  

PHP scaffolding:
  - Endpoint
  - Part
  - Builder
  - Request
  - Response
  - Response Payload
  - Logger
  - PaymentMethod

---

API issues:
  Params not consistent: card_filer is string while card_discount is array ?? similar functions
  response codes not accurate: wrong invoice number => 500 ??, other APIs return 400 sometime if there is error, others 200 with error message
  response params not consistent: trace & trace_code & ipn_trace ??
